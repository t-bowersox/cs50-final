<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{%block title required %}{% endblock %} | Todo</title>
    <link rel="icon" href="{{ url_for('static', filename='favicon.svg') }}" type="image/xml+svg">
    <link rel="alternate icon" href="{{ url_for('static', filename='favicon.png') }}" type="image/png">
    <link rel="apple-touch-icon" href="{{ url_for('static', filename='apple-touch-icon.png') }}" type="image/png">
    <link rel="stylesheet" href="{{ url_for('static', filename='styles.css') }}">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-KK94CHFLLe+nY2dmCWGMq91rCGa5gtU4mk92HdvYe+M/SXH301p5ILy+dN9+nJOZ" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-ENjdO4Dr2bkBIFxQpeoTz1HIcje39Wm4jDKdf19U8gI4ddQ3GYNS7NTKfAdVQSZe" crossorigin="anonymous"
        defer async></script>
    <script type="module">
        import { initCustomFormValidation } from '{{ url_for('static', filename='main.js') }}';

        window.addEventListener('DOMContentLoaded', () => {
            initCustomFormValidation();
        });
    </script>
    {% block scripts %}{% endblock %}
</head>

<body>
    <a href="#main" class="visually-hidden-focusable position-absolute z-1 p-3 bg-light-subtle shadow">
        Skip to main content
    </a>

    <nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="dark">
        <div class="container-fluid">
            <a href="{{ url_for('tasks.index') }}" class="navbar-brand">Todo</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbar-content"
                aria-controls="navbar-content" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbar-content">
                {% macro nav_item(name, url, active=False) %}
                <li class="nav-item">
                    {% if active %}
                    <a href="{{ url }}" class="nav-link active" aria-current="page">{{ name }}</a>
                    {% else %}
                    <a href="{{ url }}" class="nav-link">{{ name }}</a>
                    {% endif %}
                </li>
                {% endmacro %}

                {% block nav %}{% endblock %}
            </div>
        </div>
    </nav>

    <main id="main" class="container p-3">
        {% block main %}{% endblock %}
    </main>

    <footer class="container-fluid p-3 border-top text-center">
        <div>
            <small>&copy; {{ current_year() }} Tim Bowersox.</small>
        </div>
        <div>
            <small><a href="https://github.com/t-bowersox/cs50-final">View source code on GitHub</a>.</small>
        </div>
    </footer>

    {% block bottom %}{% endblock %}
</body>

</html>